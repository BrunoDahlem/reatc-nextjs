export const sizes = {
  xsmall: (theme) => theme.font.sizes.xsmall,
  small: (theme) => theme.font.sizes.small,
  medium: (theme) => theme.font.sizes.medium,
  large: (theme) => theme.font.sizes.large,
  xlarge: (theme) => theme.font.sizes.xlarge,
  xxlarge: (theme) => theme.font.sizes.xxlarge,
  huge: (theme) => theme.font.sizes.huge,
  xhuge: (theme) => theme.font.sizes.xhuge,
};
